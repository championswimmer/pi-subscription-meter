import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function (_pi: ExtensionAPI) {
  // Placeholder extension entrypoint.
  // Actual provider integrations and UI wiring will be added in later commits.
}
