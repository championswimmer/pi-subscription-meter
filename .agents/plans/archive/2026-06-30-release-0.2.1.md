# Release package version 0.2.1

- **Status:** in_progress
- **Date:** 2026-06-30
- **Owner:** agent

## Objective

Bump the package from `0.2.0` to `0.2.1`, commit the release changes, create the matching git tag, and push both the commit and tag to GitHub so npm publish can be performed manually afterward.

## Checklist

- [x] Confirm the current package version and latest git tag.
- [x] Confirm the working tree is clean before the release bump.
- [x] Update the package version to `0.2.1`.
- [ ] Commit the release changes.
- [ ] Create the `v0.2.1` git tag.
- [ ] Push `main` and `v0.2.1` to `origin`.
- [ ] Record outcomes in this plan.

## Detailed implementation plan

1. Inspect `package.json`, current git status, and existing tags to confirm the next patch version should be `0.2.1`.
2. Update versioned package metadata using the npm version workflow without auto-tagging so the release commit can be reviewed explicitly.
3. Review the resulting diff to ensure only expected release files changed.
4. Commit the version bump with a focused release message.
5. Create an annotated git tag `v0.2.1` for that commit.
6. Push the branch and the new tag to `origin`.
7. Update this plan with the final outcome and any follow-up notes for manual npm publishing.

## Risks / questions

- If `v0.2.1` already exists locally or remotely, tagging will fail.
- Version bump commands may modify both `package.json` and `package-lock.json`; review before committing.
- Pushing may fail if remote permissions or branch protections changed.

## Validation

- `git status`
- `npm version patch --no-git-tag-version`
- `git diff --stat`
- `git log --oneline -n 3`
- `git tag --list v0.2.1`
- `git push origin main`
- `git push origin v0.2.1`

## Outcome summary

In progress: version bumped to `0.2.1`; commit/tag/push pending.
